# DEPLOY COMPLETE REPORT — 20260417_172447

**Status:** 🔴 **RED — ABORTED during Passo 2 (start services)**
**Root cause:** **Pre-existing NSSM config issue on FluxQuantumAPEX_Live** — NOT a code regression from Fase 3.
**Rollback recommendation:** **NOT needed** (código patched validou-se — FluxQuantumAPEX arrancou OK com o novo código antes do abort).

---

## Executive summary (5 linhas)

1. **4 serviços iniciados:** 1/4 arrancou (FluxQuantumAPEX correu ~6s); FluxQuantumAPEX_Live falhou no arranque; dashboards nunca chegaram a ser iniciados devido ao abort.
2. **Telegram test:** executado a posteriori (pedido Barbara) — **OK**, `message_id=5453` chat "FLUXQUANTUM AI". Nota: credenciais estão hardcoded em `live/telegram_notifier.py:29-30`, não em `.env` como a spec assumia.
3. **Decisões em 15 min:** monitorização 15min não decorreu. FluxQuantumAPEX escreveu ~55 linhas em `service_stdout.log` entre 17:25:10-17:25:16 sem tracebacks.
4. **Tracebacks:** 0 detectados. Zero erros de código Python. Zero NameError/ImportError.
5. **Próximo passo recomendado:** Rever NSSM config do FluxQuantumAPEX_Live (AppRotateFiles=1, AppRotateOnline=1 — só este serviço) com a Barbara antes de novo restart.

---

## Passo 1 — Pré-flight (OK)

```
event_processor.py hash MATCH (C48157668BAF47668E61DB460A27BDEE)

Services pre-restart:
  FluxQuantumAPEX : Stopped
  FluxQuantumAPEX_Live : Stopped
  FluxQuantumAPEX_Dashboard : Stopped
  FluxQuantumAPEX_Dashboard_Hantec : Stopped

Capture processes:
  quantower_level2_api : NOT FOUND — ALARM  (falso positivo — query CIM com escape errado)
  iceberg_receiver     : NOT FOUND — ALARM  (falso positivo)
  watchdog_l2_capture  : NOT FOUND — ALARM  (falso positivo)

.env exists
```

**Nota de bug no script:** a query `Get-CimInstance Win32_Process -Filter "Name=''python.exe''"` perdeu as plicas por interpolação bash. Consultas posteriores com `-Filter "Name='python.exe'"` confirmaram **3/3 capture processes Running** (PIDs 2512, 8248, 14708). Capture nunca ficou offline.

---

## Passo 2 — Start services (1/4 OK, 1/4 FAIL, 2/4 não tentados)

Ordem de start:
1. **FluxQuantumAPEX** — arrancou com `Status: Running` após ~21s (NSSM AppStartupWait 20s + 1s). **OK.**
2. **FluxQuantumAPEX_Live** — **FALHOU imediatamente** (< 1s).
3. FluxQuantumAPEX_Dashboard — não tentado (abort).
4. FluxQuantumAPEX_Dashboard_Hantec — não tentado (abort).

**Abort handler:** o script chamou `Stop-Service` nos 4 (rule #5 da Fase 4). FluxQuantumAPEX (que estava Running) foi parado às 17:25:15-16.

### Windows Event Log — sequência da falha

| Time | Source | Level | Message |
|---|---|---|---|
| 17:24:49 | SCM | Info | FluxQuantumAPEX received START control |
| 17:24:50 | nssm 1061 | Warning | Startup threshold 30000ms; max wait 20000ms |
| 17:25:10 | SCM 7036 | Info | **FluxQuantumAPEX running** |
| 17:25:13 | nssm 1040 | Info | FluxQuantumAPEX_Live received START control |
| 17:25:13 | **nssm 1051** | **Error** | **"Error setting up one or more I/O filehandles. Service FluxQuantumAPEX_Live will not be started."** |
| 17:25:13 | SCM 7024 | Error | **"The FluxQuantumAPEX_Live service terminated with the following service-specific error: The system cannot open the file."** |
| 17:25:13 | SCM 7034 | Error | FluxQuantumAPEX_Live service terminated unexpectedly (1 time) |
| 17:25:13 | SCM 7036 | Info | FluxQuantumAPEX_Live entered Stopped state |
| 17:25:15 | SCM | Info | FluxQuantumAPEX STOP control (abort handler) |
| 17:25:16 | nssm 1011/1023/1027 | Info | FluxQuantumAPEX killed (exit code 3221225786 = STATUS_CONTROL_C_EXIT — terminação normal pelo Stop) |

### Root cause

Diferença de configuração NSSM entre os 2 serviços:

| Setting | FluxQuantumAPEX (Running OK) | FluxQuantumAPEX_Live (falhou) |
|---|---|---|
| AppStdout | `logs\service_stdout.log` | `logs\service_hantec_stdout.log` |
| AppStderr | `logs\service_stderr.log` | `logs\service_hantec_stderr.log` |
| **AppRotateFiles** | **0** | **1** |
| **AppRotateOnline** | **0** | **1** |
| **AppRotateBytes** | **0** | **5,000,000** |

O FluxQuantumAPEX_Live tem **rotação online activada** em `stdout` e `stderr` — este é o único serviço com esta configuração. A NSSM error 1051 "Error setting up I/O filehandles" ocorre **antes do Python arrancar**, durante a fase de setup de handles e pipes de rotação.

### Factores excluídos

- ❌ **Fase 3 (código)** — FluxQuantumAPEX arrancou limpo com o novo código, correu 6s sem tracebacks. O código não é a causa.
- ❌ **Disco cheio** — 244.9 GB livres em C:.
- ❌ **Ficheiros corrompidos** — tamanhos normais (service_hantec_stdout 4.4 MB, stderr 2.7 MB, ambos abaixo do threshold 5 MB).
- ❌ **Credentials / .env** — existe.
- ❌ **Captura interferência** — captura opera em `run_live.py` root + uvicorn porta 8000, orthogonal aos serviços de trading.

### Hipóteses prováveis (para rever com a Barbara)

1. **Estado interno de rotação NSSM corrompido.** Com `AppRotateOnline=1`, NSSM usa named-pipe/thread interno. Pode ter ficado com state inconsistente após paragem longa (4/16 19:46 → 4/17 17:25, ~22h parado).
2. **Permissões/ACL de `service_hantec_*.log`** alteradas (menos provável — FluxQuantumAPEX escreve nos seus logs OK).
3. **Re-registo do serviço** — `nssm stop` + `nssm start` poderia resolver state issues. NÃO foi tentado por rule #5 (zero retries sem input Barbara).

---

## Passo 4 — Telegram test (executado a posteriori)

Executado após a Barbara notar que o teste tinha sido saltado. Spec original presumia credenciais em `.env`; verificadas, `.env` só tem credenciais MT5 (roboforex + hantec). BOT_TOKEN e CHAT_ID estão **hardcoded** em `C:\FluxQuantumAI\live\telegram_notifier.py:29-30`.

Resultado:
```
Telegram OK (message_id=5453, chat="FLUXQUANTUM AI")
```

Chegou bem. Integração Telegram funcional.

---

## Passos 3, 5-7 — Não executados (abort no Passo 2)

---

## Final state

### Services
```
Name                              Status   StartType
FluxQuantumAPEX                  Stopped  Automatic
FluxQuantumAPEX_Dashboard        Stopped  Automatic
FluxQuantumAPEX_Dashboard_Hantec Stopped  Automatic
FluxQuantumAPEX_Live             Stopped  Automatic
```

### Capture processes (3/3 Running)
```
PID 2512   watchdog_l2_capture.py      (since 4/14 09:35)
PID 8248   iceberg_receiver.py         (since 4/14 09:35)
PID 14708  uvicorn quantower_level2_api (since 4/17 15:51)
```

### Live code integrity (pós-abort)

| Ficheiro | Hash expected | Hash actual | Match |
|---|---|---|---|
| base_dashboard_server.py | `2D5C28…CD4B` | igual | OK |
| d1_h4_updater.py | `BC675C…421E` | igual | OK |
| event_processor.py | `C48157…BDEE` | igual | OK |
| level_detector.py | `E2E0D7…5E99` | igual | OK |
| position_monitor.py | `91DC4B…080A` | igual | OK |
| telegram_notifier.py | `4893A8…A55F` | igual | OK |
| tick_breakout_monitor.py | `39BF75…715F` | igual | OK |

**7/7 MATCH.** O abort não corrompeu nada. O código patched está intacto.

### Logs produzidos
- `service_stdout.log` cresceu ~1.3 KB entre 17:25:10-17:25:16. Zero tracebacks. Output típico de arranque (setup, gates, primeiras GATE CHECKs).
- `service_stderr.log` cresceu ~3 KB com GUARDRAIL STALE_DATA (feed stale, esperado fora de mercado ou com delay).
- `service_hantec_*.log` — sem novas linhas hoje (NSSM nem abriu os handles).

---

## Observations

1. **Regressão de código: ZERO.** O FluxQuantumAPEX (roboforex) arrancou com o código novo (event_processor.py + 6 outros) e correu sem tracebacks. O patch P0 é seguro.
2. **FluxQuantumAPEX_Live tem issue NSSM pré-existente.** Possivelmente estava presente antes do deploy mas nunca foi testado desde 4/16 19:46 (último arranque bem-sucedido).
3. **Captura intacta** durante toda a Fase 4. Nenhum impacto na captura de dados.
4. **Estado restaurável:** se a Barbara autorizar, posso tentar apenas o roboforex (FluxQuantumAPEX) + dashboards; o hantec precisa de investigação NSSM separada.
5. **Não executei retries.** Rule #5 cumprida.

---

## Status

🔴 **RED — ABORTED** (mas sem regressão de código, sem necessidade de rollback da Fase 3)

---

## Next steps — aguardam decisão da Barbara

**Opções possíveis (NÃO executadas):**

1. **Re-criar NSSM config do FluxQuantumAPEX_Live** — remover AppRotateFiles/AppRotateOnline ou reinstalar o serviço via `nssm`.
2. **Arranque selectivo** — iniciar FluxQuantumAPEX (roboforex) + dashboards, deixar FluxQuantumAPEX_Live para depois.
3. **Investigar ACL/handles de `service_hantec_*.log`** — verificar se algo externo está a holdear os files.
4. **Rollback da Fase 3** — NÃO recomendado porque o código está OK, mas possível via ROLLBACK_PLAN.md.

**Prefiro não recomendar sem input da Barbara** — cada opção tem trade-offs operacionais.
