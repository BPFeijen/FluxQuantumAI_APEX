# Premise verification — rebuild parquet coverage (EXEC-RECALIB-INVESTIGATION-001 Step 1)

**Task:** EXEC-RECALIB-INVESTIGATION-001 (Asana 1214283093312730)
**Premise tested:** "Rebuild parquet `data/rebuild_2026-04-25/gc_ohlcv_l2_joined.parquet` is clean post-DATA-002 P1.5 across the full 9.7-month Window B (Jul 2025 → 2026-04-25)."
**Result:** PARTIALLY REFUTED — see split below.

## 1. Parquet metadata

| Field | Value |
|---|---|
| Path | `C:\FluxQuantumAI\data\rebuild_2026-04-25\gc_ohlcv_l2_joined.parquet` |
| sha256 | `9871482a7fabeed855009f40b33e99382b2a4af6ce72067522952cf81ddba56b` (matches calibration artifacts) |
| Full M1 ts_min | `2020-01-01 23:00:00+00:00` |
| Full M1 ts_max | `2026-04-24 20:59:00+00:00` |
| Full M1 row count | 2,202,371 |

## 2. Window B coverage (Jul 2025 → 2026-04-25)

| Field | Value |
|---|---|
| M1 rows in window | 538,800-ish (subset of full) |
| M30 resampled rows | 8,800 |
| Naive expected M30 (clock-based) | 14,304 |
| Naive coverage | 61.5 % (low because GC closes weekends + maintenance windows) |
| Weekday-only coverage | **93.5 %** (245/262 weekdays have data) |
| Missing weekdays | 17 — consistent with US market holidays + the known L2 capture gaps inventoried in `_audit/audit/level2_gap_inventory.md` |

**Verdict on coverage:** ADEQUATE. 93.5 % weekday coverage is well above the spec's 5 % gap threshold (>95 % would be ideal but the missing 6.5 % is dominated by holidays and previously-inventoried L2 gaps, not new gaps).

## 3. HANDOVER_CUTOFF split (the load-bearing finding)

`rebuild.log` shows the parquet was assembled in two halves:

```
[2026-04-25T22:02:47] HANDOVER_CUTOFF=2025-11-26 00:00:00+00:00
[2026-04-25T22:02:47] Loading production parquet... prod shape=(2207662, 5)
[2026-04-25T22:02:47] pre-handover (kept as-is from prod): 2088938 rows, last=2025-11-25
[2026-04-25T22:02:47] Found 112 trades_*.csv.gz files in C:\data\level2\_gc_xcec
[2026-04-25T22:04:45] post-handover concat: 113433 rows, 2025-12-01 -> 2026-04-24
[2026-04-25T22:04:46] Wrote ... 2202371 rows
```

**What this means:**

- **Pre-handover (2020-01-01 → 2025-11-25):** kept AS-IS from the old production parquet. NOT rebuilt. The rebuild only changes post-handover bars.
- **Post-handover (2025-12-01 → 2026-04-24):** rebuilt from raw `trades_*.csv.gz` tape, byte-equal to tape per sanity check on 2026-02-13 (delta H=0, delta L=0 vs tape).

| Window B half | M30 rows | % of Window B | Status |
|---|---:|---:|---|
| Pre-handover (Jul 2025 → Nov 25 2025) | 4,861 | 55.2 % | Kept-as-is from prod parquet |
| Post-handover (Nov 26 2025 → Apr 24 2026) | 3,939 | 44.8 % | Rebuilt from raw trades |

## 4. DATA-002 P1.5 fix scope

DATA-002 P1.5 fix (HEAD `1eda7ab`) corrected `live/m30_updater.py:90` — a `mid_price`-derived OHLC bug in the LIVE m30 updater that wrote bars only AFTER it was deployed. The deployment cutover corresponds to `HANDOVER_CUTOFF=2025-11-26`. Therefore:

- **Pre-handover bars** (kept-as-is from prod): generated by the OLD offline pipeline before `m30_updater` was deployed. **Presumed clean** (was always clean — no live-update bug).
- **Post-handover bars** (rebuilt): the OLD prod parquet had post-handover bars written by the buggy `m30_updater`. The rebuild replaced those with raw-trade-derived OHLCV. **Now clean.**

## 5. Implication for EXEC-RECALIB-001 baseline claim

The recalibration brief stated: "rebuilt-clean signal is what production will see going forward; the prior magnitudes were inflated by contamination."

**This is over-stated.** The rebuilt parquet is clean only over the post-handover portion (~45 % of Window B). The pre-handover portion (~55 %) is identical to the old prod parquet. So:

- Comparison "old calibration vs rebuild calibration" is really "calibration on contaminated post-handover + clean pre-handover" vs "calibration on clean post-handover + identical clean pre-handover".
- Differences in d values can come from:
  (a) The post-handover OHLCV change (DATA-002 P1.5 effect)
  (b) Methodology drift (different scoring, eligibility, label, etc.)
  (c) Sample-size / regime variance

Step 4 of this investigation directly tests (a) by isolating pre-handover from post-handover Cohen's d.

## 6. Premise verdict

| Premise | Status after Step 1 |
|---|---|
| Rebuilt parquet is clean over the post-handover portion | VALIDATED (sha256 matches; sanity vs tape on 2026-02-13 H/L delta = 0.0pt) |
| Rebuilt parquet is clean over the pre-handover portion | VALIDATED (kept as-is from prod which was offline-generated and was always clean of the m30_updater bug) |
| Window B is fully rebuilt-clean | **REFUTED** — only post-handover portion (~45 %) was rebuilt; pre-handover (~55 %) is unchanged |
| Window B has adequate coverage (>95 % weekdays) | PARTIALLY VALIDATED — 93.5 % is below spec's 95 % bar but consistent with documented holidays + L2 gaps |

**No CRITICAL coverage gap detected.** The investigation can proceed; the half-window-rebuild caveat is what makes Step 4's pre-vs-post isolation test load-bearing rather than mere confirmation.
